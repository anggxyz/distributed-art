export declare enum SignMethod {
    ETH_SIGN = 0,
    TREZOR = 1,
    EIP712 = 2,
    PERSONAL_SIGN = 3
}
export interface RpcResponse {
    id: number;
    jsonrpc: string;
    result: any;
}
export interface SendOptions {
    method: string;
    params: any[];
    id?: number;
    jsonrpc?: string;
}

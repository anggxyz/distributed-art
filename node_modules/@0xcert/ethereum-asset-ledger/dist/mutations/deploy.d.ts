import { GenericProvider, Mutation } from '@0xcert/ethereum-generic-provider';
import { AssetLedgerDeployRecipe } from '@0xcert/scaffold';
export default function (provider: GenericProvider, { name, symbol, uriBase, schemaId, capabilities }: AssetLedgerDeployRecipe): Promise<Mutation>;
